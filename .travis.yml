language: go
go:
  - 1.11.x

before_install:
  - go get github.com/mitchellh/gox
  - go get github.com/tcnksm/ghr

script:
  - echo "hello"

after_success:
  - gox -osarch="linux/amd64 windows/amd64" -output "dist/{{.OS}}_{{.Arch}}_{{.Dir}}"
  - ghr --delete --replace --prerelease --debug pre-release dist/
